apiVersion: apps.operator.com/v1
kind: Nodepool
metadata:
  name: master
spec:
  # Add fields here
  taints: 
    - key: node-pool.test
      value: test
      effect: NoSchedule
  labels:
    "node-pool.lailin.xyz/test": "2"
    "node-pool/master": "10"
  ncl: runc
